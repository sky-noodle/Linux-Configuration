#!/bin/bash

#sudo apt-get install xclip
#sudo easy_install -ZU autopep8 
cd ~
echo "Ljc正在为您安装相关程序" > Ljc
sudo apt-get install vim-gtk ctags cscope astyle git subversion

# backup
rm -rf ~/vim_ljc ~/.vim_old
mv -f ~/.vim ~/.vim_old
mv -f ~/.vimrc ~/.vimrc_old

# new files
echo "请耐心等待" >> Ljc
svn checkout https://github.com/Will-Grindelwald/Linux-Configuration/trunk/Vim ~/vim_ljc
mv -f ~/vim_ljc ~/.vim
mv -f ~/.vim/.vimrc ~/.vimrc
rm -rf ~/.vim/.svn ~/.vim/README.md ~/.vim/setup

# Plugin
echo "即将安装完成" >> Ljc
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
vim Ljc +PluginInstall +qall
vim Ljc -c "helptags ~/.vim/doc" -c q
rm Ljc
echo "安装完成"

